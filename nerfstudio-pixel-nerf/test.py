from PixelNeRFConfig import PixelNeRF 

if __name__ == "__main__":
    # Test if the config can be instantiated without errors
    config = PixelNeRF.config
    print("PixelNeRF configuration instantiated successfully.")
